#!/usr/bin/env node
// Cloudinary cleanup script removed.
// This project no longer includes Cloudinary integration. If you previously used Cloudinary,
// consider migrating any manual cleanup steps you had to your Cloudinary console.
// For local uploads cleanup, use `node scripts/orphaned_uploads_report.js` which reports
// files in ./uploads that are not referenced by the database.

console.log('cleanup_cloudinary.js has been removed. Use scripts/orphaned_uploads_report.js for local upload cleanup.');
